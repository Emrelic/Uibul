<Window x:Class="UIElementInspector.Windows.PieProgressWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Progress"
        Width="100" Height="110"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True"
        ShowInTaskbar="False"
        ResizeMode="NoResize"
        IsHitTestVisible="False">

    <!-- Semi-transparent rounded container -->
    <Border CornerRadius="10" Background="#CC222222" Padding="6" Margin="3">
        <Border.Effect>
            <DropShadowEffect BlurRadius="6" ShadowDepth="1" Opacity="0.4" Color="Black"/>
        </Border.Effect>

        <StackPanel HorizontalAlignment="Center">
            <!-- Pie Chart Area -->
            <Grid Width="50" Height="50" HorizontalAlignment="Center">
                <!-- Background track circle -->
                <Ellipse Width="50" Height="50"
                         Stroke="#55FFFFFF" StrokeThickness="0"
                         Fill="#33FFFFFF"/>

                <!-- Pie progress arc (drawn in code-behind) -->
                <Path Name="pieArc" Fill="#1976D2" HorizontalAlignment="Center" VerticalAlignment="Center"/>

                <!-- Inner circle overlay for donut effect -->
                <Ellipse Width="32" Height="32"
                         Fill="#CC222222"
                         HorizontalAlignment="Center" VerticalAlignment="Center"/>

                <!-- Percentage text in center -->
                <TextBlock Name="txtPercent" Text="0%"
                           Foreground="White" FontSize="11" FontWeight="Bold"
                           HorizontalAlignment="Center" VerticalAlignment="Center"/>

                <!-- Completion checkmark (hidden by default) -->
                <TextBlock Name="txtCheckmark" Text="&#x2714;"
                           Foreground="#4CAF50" FontSize="20" FontWeight="Bold"
                           HorizontalAlignment="Center" VerticalAlignment="Center"
                           Visibility="Collapsed"/>
            </Grid>

            <!-- Step text -->
            <TextBlock Name="txtStep" Text=""
                       Foreground="#BBBBBB" FontSize="9"
                       HorizontalAlignment="Center" Margin="0,3,0,0"/>

            <!-- Status message -->
            <TextBlock Name="txtMessage" Text=""
                       Foreground="#999999" FontSize="8"
                       HorizontalAlignment="Center" Margin="0,1,0,0"
                       TextTrimming="CharacterEllipsis" MaxWidth="85"
                       TextAlignment="Center"/>
        </StackPanel>
    </Border>
</Window>
